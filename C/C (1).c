#include <stdio.h>

int main(void) {
  //codigo retirado direto da apostila (copia e cola)
  printf(“Digite o %iº valor do vetor:”, i+1);

  //codigo reescrito direto usando a apostila (sem copia e cola)
  printf("Digie o %i valor do vetor:", i+1);
  return 0;
}